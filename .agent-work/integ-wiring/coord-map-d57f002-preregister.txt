COORD-MAP d57f002 PRE-REGISTER (written BEFORE quartus_map)
===========================================================
Freeze: d57f002 (rtl/i-slice-luma) — h264_byte_ram_sp + restructured traverse
Correctness same tree (traverse claim): clip1 300/300 y_px, clip2 1170/1170, true rc=0
Build: /home/flynnsbit/mplex-builds/coord-map-d57f002/Plex_MiSTer
Method: product wire6 base + map_decode_area_probe (same anti-prune as map2d)
Prior bomb (map2d @ 6dc5993+attrs): traverse 1,180,271 comb ALUTs; chip 1,241,952 ALMs needed

INTEGRATE PREDICTIONS (own — not traverse's 800..3000 alone)
----------------------------------------------------------
1) h264_byte_ram_sp M10K inference: YES
   bits: 65,536 (DEPTH=8192×8 via MAX_RBSP_BYTES)
   basis: micro-probe textbook A HIT (45af397); module is same 1R1W registered-q pattern

2) h264_p_mb_traverse area:
   comb ALUTs: 2,000 .. 8,000
   map ALMs needed (entity or self+children proxy): 1,200 .. 4,500
   point estimate: ~2,500 ALMs / ~4,000 comb ALUTs
   GATE: ≤ 3,000 ALMs (traverse pre-reg 800..3000) — I am NOT sure of sub-3k; call borderline

3) Whole-design ALMs needed:
   ~45,000 .. 70,000  (sink still ~40k comb in probe; product ~21k)
   NOT under 41,910 until sink crash-diet — traverse fix alone does not make chip fit

4) DSP: ~140 .. 160 (sink parallel IQ still present; similar map2d 149 / map3 154)
5) Block memory bits: product ~3.0M + RBSP 65k + any inferred neighbours
   M10K: RBSP must show in RAM summary as M10K

FALSIFIERS
- byte_ram block memory bits = 0 → inference FAIL (then micro-probe already explains edge)
- traverse comb ALUTs still >100k → access restructure did not take / probe pruned wrong module
- traverse ALMs >3000 → MISS gate (publish MISS; still huge win vs 1.18M)

PASS hope: M10K YES + traverse ≤3000 ALMs + exact-source gap CLOSED on record.
