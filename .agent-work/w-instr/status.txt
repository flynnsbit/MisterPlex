# w-instr status — avsync harden + bimodality + ledger

branch: w-avsync-hdmi-measure
code tip: git rev-parse --short HEAD

## Task 1 — avsync_measure_hdmi.py (ONLY working lipsync judge)
- DEFAULT_PAIR_WINDOW_S=0.9 (correct default; 0.45 mispairs)
- DEFAULT_WARMUP_FRAMES=20; live min decoded frames=50 → else UNSCORED
- LIMITATIONS banner every run: absolute lipsync CANNOT_MEASURE (raw_uncalibrated);
  same-rig deltas CAN; av_drift_ms BLIND
- PASS/FAIL refused (rc=77 REFUSE_DEFAULT_ASSUMED) when tol_ms is DEFAULT_ASSUMED
  unless --allow-default-score or --tol-ms supplied (ERROR 17)
- No 23.976 anywhere in this tool
- unit: tests/unit/test_avsync_measure_hdmi.sh true rc=0 (28 pass incl refuse_default)

## Task 2 — tools/avsync_bimodality.py
- Data-driven 1-D two-means; NO hardcoded A/B centres
- BIMODAL rc=2, UNIMODAL rc=0, UNSCORED rc=77 (min n/cluster)
- first_* carries run_index + first_pair_t_flash_s
- RBG: parent-scale ~117ms → rc=2; tight → rc=0; n=1 → 77

## Task 3 — tools/frame_ledger_report.py
Full single-session ledger:
  residual = frames - presents - drops
  drops = A/V-pacer ONLY; publish_misses separate
Parent command (after pulling ledger/log — agent does not touch device):

  python3 tools/frame_ledger_report.py \
    --ledger /path/to/misterplexd.frame_ledger \
    --require-explained; echo "true rc=$?"

  # or from daemon log:
  python3 tools/frame_ledger_report.py --log /path/to/misterplexd.log; echo "true rc=$?"

Device path typically: /media/fat/misterplex_v2/misterplexd.frame_ledger

## Evidence
python3 tools/avsync_bimodality.py --self-test; echo "true rc=$?"  → 0
python3 tools/frame_ledger_report.py --self-test; echo "true rc=$?" → 0
bash tests/unit/test_avsync_measure_hdmi.sh; echo "true rc=$?" → 0
