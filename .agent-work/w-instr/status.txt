# w-instr status — origin record decisive + held_ms variance gate

branch: w-avsync-hdmi-measure
code_SHA: 5dd06733  tip: git rev-parse --short HEAD  branch: w-avsync-hdmi-measure

## ARM daemon (parent deploys)
path: build/arm/misterplexd
md5: aaa726c7c5bcb733e8d9b08b3fe051ca
prev_deployed: 5996385a57c6af142b8e732a39b36a4a  (parent confirmed live; lacks this turn's fields)

## What changed (this commit)
1. Explicit hold metrics on daemon lines (no hand mono subtract):
   - first_audio_pcm: mono_ms + wall_s + tag=measured
   - A/V audio_release: held_ms + hold_wall_ms + mono_ms + tag=measured
   - pump audio release: held_ms + hold_wall_ms + past_bias + since_* + tag=measured
   - first_video_present: since_first_audio_pcm_ms + since_audio_release_ms + since_pump_release_ms
   - MrAudio playback position available: mono_ms + since_origin_ms + since_first_audio + since_audio_release (fallback window length)
   - audio latency samples: mono_ms + since_origin_ms (anti sampling-phase trap)

2. Peer-aligned hold drain (av_clock.hpp + media_player):
   - non-empty hold → past_bias=0 (hold_drain_no_past_bias=1)
   - empty-hold open may still past-bias kFeedTargetMs
   - makeHoldSessionReport / holdDrainShouldPastBias first-class

3. tools/held_ms_stability_gate.py — multi-session variance gate
   - FAIL rc=4 when range(hold_wall_ms) > --max-range-ms (default 40 DEFAULT_ASSUMED)
   - NO-DATA rc=77 if n < min_sessions
   - measured FAIL never decays to 77

4. tools/startup_window_sample.py — ORIGIN_EXPLICIT + CLUSTER_AXIS parse/print

## Evidence (true rc DIRECT)
python3 tools/held_ms_stability_gate.py --self-test; echo "true rc=$?"  → 0
python3 tools/startup_window_sample.py --self-test; echo "true rc=$?" → 0
python3 tools/held_ms_stability_gate.py --values 120 200 120 --min-sessions 3 --max-range-ms 40
  → HOLD_VARIANCE_FAIL true rc=4
python3 tools/held_ms_stability_gate.py --values 118 120 119 121 --min-sessions 3 --max-range-ms 40
  → HOLD_STABLE true rc=0
python3 tools/held_ms_stability_gate.py --values 120
  → NO-DATA true rc=77
make arm-plexd; echo "true rc=$?" → 0
make unit; echo "true rc=$?" → 0
strings: hold_wall_ms / MrAudio available mono_ms / hold_drain_no_past_bias present

## Parent recipes (host only; parent owns device)
# Deploy new binary then:
python3 tools/startup_window_sample.py --hz 10 --duration 12 --log /tmp/sws_run.log
# After n>=3 instrumented sessions, gate on hold_wall_ms:
python3 tools/held_ms_stability_gate.py --values <h1> <h2> <h3> ... --min-sessions 3 --max-range-ms 40; echo "true rc=$?"
# Or from logs:
python3 tools/held_ms_stability_gate.py --log /tmp/sws_*.log --min-sessions 3; echo "true rc=$?"

## Labels discipline
Every value: measured | caller_supplied | DEFAULT_ASSUMED | NO-DATA
DEFAULT max_range_ms=40 is DEFAULT_ASSUMED (design half of ~117 ms cluster sep) — override with --max-range-ms for caller_supplied.
