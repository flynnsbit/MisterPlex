# w-instr — P5 frame ledger / session_epoch

branch: w-avsync-hdmi-measure
tip: (git rev-parse HEAD)

## ARM binary
path: build/arm/misterplexd
md5: a9579a4294a64043ff271baebddc4f1e
make arm-plexd true rc=0

## What shipped
1. session_epoch = process_epoch.stream_seq
   - process_epoch: armed once at daemon start (steady mono_ms)
   - stream_seq: bumped at every play()/stream START
   - emitted on 1 Hz media line + session end + stream_start
2. Per-stream counters unchanged; lifetime_* + lifetime_unaccounted on every line
3. unaccounted = frames - presents - drops printed every telemetry line
   (residual synonym kept). unaccounted_eq=frames-presents-drops tag=measured
4. B5: teardown asserts totalBytes % frameBytes==0 + rawPipeDesynced;
   ERROR media: PIPE_DESYNC=1 hard trip
5. B2: MEASURED_DELIVERY already -loglevel info; tag=measured on lines;
   1 Hz measured_delivery=WxH measured_delivery_src=measured|NO-DATA

## Host gate
python3 tools/frame_ledger_report.py --log DAEMON.log --ledger LEDGER \
  --require-explained; echo "true rc=$?"
- multi session_epoch in window → LEDGER_FAIL rc=2
- unaccounted != publish_misses with --require-explained → rc=2
- clean → rc=0

## Deploy (parent only)
REMOTE_ROOT=/media/fat/misterplex_v2 ./scripts/deploy_misterplexd_pid_safe.sh
# expect LIVE md5 a9579a4294a64043ff271baebddc4f1e via readlink -f /proc/PID/exe

## Pre-registered predictions (next soak)
- Clean single-session soak: unaccounted ≈ publish_misses (often 0);
  session_epoch stable for whole cast; lifetime_* monotonic; process_epoch fixed.
- If daemon self-exits mid-soak: process_epoch changes → frame_ledger_report
  with --log MUST LEDGER_FAIL multi_session_epoch (P4 visible).
- If ~16 frames still missing vs 24.000 source while drops=1: unaccounted grows
  and is NOT explained by publish_misses → --require-explained FAIL (S2 named).
