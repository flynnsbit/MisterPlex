# w-instr status — absorb parent instrument regressions + fingerprint gate

branch: w-avsync-hdmi-measure
tip: (see git rev-parse HEAD)

## Parent regressions absorbed (rule-0)
1. DEFAULT_CAP_FPS was 60 without measuring MS2109 caps → every capture failed, hid as rc=77.
   NOW: DEFAULT_CAP_FPS=30.0; preflight _supported_cap_fps(); unsupported → INSTRUMENT_BROKEN rc=3.
2. -copyts without -start_at_zero truncated captures (33KB vs 6.28MB) → INSTRUMENT_BROKEN on CAPTURE_FAILED.
3. rc=77 must not mean "instrument broken". Ladder: PASS=0 FAIL=2 BROKEN=3 UNSCORED=77.

## Historical dataset INVALID
Corrected wallclock alignment shifted median ~90 ms (-105 vs old -195/-312).
Bimodality must be re-established on fingerprinted captures only.

## Capture-config fingerprint
JSON field capture_config.ffmpeg_argv + fingerprint=sha256:...
avsync_bimodality HARD FAIL rc=3 MIXED_CAPTURE_CONFIG on divergent or missing fingerprints.
Never decays to 77.

## Deploy order (supervisor-safe)
stage → cp prev → mv onto live path → kill captured PID → SUPERVISOR restart →
readlink -f /proc/PID/exe + md5 THAT path. No manual start by default.
scripts/deploy_misterplexd_pid_safe.sh

## ARM (unchanged this turn)
md5 a9ade50b18c50feff08021ba4ab12e87 still the six-field build parent deployed.

## Self-tests (true rc direct)
python3 tools/avsync_measure_hdmi.py --self-test; echo "true rc=$?"   # 0
python3 tools/avsync_bimodality.py --self-test; echo "true rc=$?"     # 0
# CLI: same-fp JSON → BIMODAL rc=2; mixed-fp → MIXED_CAPTURE_CONFIG rc=3
