# w-instr status — parent attack response (plateau WARN + RATE honesty + overlay readback)

branch: w-instr-motion-counter

## Delivered

### Attack 1 — max_plateau=3/3 at bound
- Hard fail remains only max_plateau > ceil(cap/src)+1 (allowed=3 at 24/30).
- max_plateau == allowed is still PASS (healthy cal maxed at 2; 3 is one unlucky sample).
- Emits plateau_warn=1 + rate_notes when riding the bound so it is never silent green.
- Form ceil(cap_fps/src_fps)+1 kept: ideal hold is <=ceil(cap/src); +1 is single-sample slack.

### Attack 2 — RATE_OK never on DEFAULT_ASSUMED
- RATE_OK requires both fps authoritative:
  - src: caller (--source-fps / --src-fps / daemon token 24/1)
  - cap: caller or container (--capture-fps / --cap-fps / --probe-capture ffprobe)
- Otherwise rate dimension = RATE_UNSCORED (never RATE_OK). Metrics still printed with provenance.
- Ratio/endpoint/plateau gates only fire when authoritative.
- Revisit (bank-swap) still RATE_FAIL without fps — pure measured sequence property.
- FREEZE still hard rc=1 when counter pinned (do not soften).

### Overlay string read-back (user #1 acceptance)
- New: tools/overlay_string_readback.py
- Exact glyph recovery of expected string (default STOPPED) via 5x7 bitmaps from
  playback_overlay.hpp — NOT lattice-pitch.
- RED on real overlay_lowres_evidence.png: READBACK_FAIL rc=1 decoded='4TNEEE0'
- RED on synth STORE_Y_SCALE=2 row-delete: READBACK_FAIL rc=1 decoded='STOPP4Y'
- GREEN on synth clean: READBACK_OK rc=0 decoded='STOPPED'
- Motion instrument still UNSCORED rc=77 on that evidence (no TREK counter) — correct.

## Commands for parent

python3 tools/hdmi_motion_instrument.py CAP_DIR --src-fps 24/1 --cap-fps 30
# Without fps flags -> motion may be OK but rate=RATE_UNSCORED

python3 tools/overlay_string_readback.py FRAME.png --expect STOPPED
python3 tools/overlay_string_readback.py --synth-row-delete --expect STOPPED   # expect rc=1
python3 tools/overlay_string_readback.py --self-test

./tests/hw/test_hdmi_motion_instrument.sh

## Evidence (true rc, direct)

| case | result |
|------|--------|
| suite targeted | pass=34 fail=0 skip=1 RESULT=PASS true rc=0 |
| evidence overlay readback | READBACK_FAIL rc=1 decoded=4TNEEE0 |
| synth clean readback | READBACK_OK rc=0 |
| synth row-delete | READBACK_FAIL rc=1 |
| capboot no fps | MOTION_OK rc=0 rate=RATE_UNSCORED src=DEFAULT_ASSUMED |
| capboot +fps | MOTION_OK rc=0 rate=RATE_OK fps_authoritative=1 endpoint=0.7982 expected=0.8 |
| cap480a | STRUCTURE_FAIL rc=3 |
| cap480n | UNSCORED rc=77 |
| half-rate synth | RATE_FAIL rc=4 |

## Hardcoded constants audit
- DEFAULT_ASSUMED_SOURCE_FPS=24.0 — assumed, labelled
- DEFAULT_ASSUMED_CAPTURE_FPS=30.0 — assumed, labelled; PNG dirs cannot measure fps
- max_plateau_allowed formula — design (parent cal)
- CELL_SCORE_MIN/STRING_SCORE_MIN — design for overlay readback
