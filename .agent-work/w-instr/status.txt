# w-instr status — startup-window sampler + provenance refuse + severity proof
updated: 2026-07-31
branch: w-avsync-hdmi-measure
SHA: 9f148d26

## TASK 1 — startup-window instrument
tools/startup_window_sample.py
- Polls local log at >=10 Hz (--live --hz 10 --duration 12) or --offline
- Does NOT ssh; parent feeds log (tail -F misterplexd.log > local.log)
- Session gate: wall_s advancing (sws_arm shape). Absence = NO-DATA rc=77, never 0.0
- Per-drop wall_s events (not a total)
- first_video_av_drift_ms extracted as lines arrive (before any clear)
- audio release vs first video
- residual = frames - presents - drops checked every sample
- Self-test: empty→77, synthetic 5 drops→0, ledger break→2

Daemon (parent deploy): arm/misterplexd/media_player.cpp
- EVERY A/V resync drop logs wall_s + drift + drops + frames + presents
- first_video_present wall_s + av_drift_ms + audio_s (once)

Parent recipe:
  # on host, before cast:
  ssh ... 'tail -n0 -F /media/fat/misterplex_v2/misterplexd.log' > /path/startup.log &
  python3 tools/startup_window_sample.py --log /path/startup.log --live --hz 10 --duration 15 -o startup.json
  # cast during the window; then:
  python3 tools/startup_window_sample.py --log /path/startup.log --offline --json

## TASK 2 — provenance (ERROR 17)
- PROVENANCE_CALLER=caller_supplied; default src 24.000 not 23.976
- RATE_OK never on DEFAULT_ASSUMED
- --strict-fps → VERDICT=REFUSE_DEFAULT_ASSUMED rc=77 when fps assumed
  BUT measured hard fails (3/2/4/1) never decay to 77
- RED: half-rate + DEFAULT_ASSUMED → RATE_UNSCORED (not wrong RATE_OK)
  GREEN: half-rate + caller fps → RATE_FAIL
- scripts/source_rate_rca.sh default FPS 24/1 labelled DEFAULT_ASSUMED (was 24000/1001)

## TASK 3 — severity ladder (true rc DIRECT, no pipe)
  /tmp/cap480a --source-fps 24 --capture-fps 30
    VERDICT=STRUCTURE_FAIL rc=3  true rc_a=3
    (color also GREEN+CHROMA+GREYSCALE; structure wins)
  /tmp/cap480b --source-fps 24 --capture-fps 30
    VERDICT=MOTION_OK rc=0  true rc_b=0
  /tmp/cap480b --strict-fps (no --source-fps)
    VERDICT=REFUSE_DEFAULT_ASSUMED rc=77  true rc_strict=77

## Self-tests
  python3 tools/startup_window_sample.py --self-test; echo "true rc=$?"  → 0
  python3 tools/hdmi_motion_instrument.py --self-test; echo "true rc=$?" → 0

## Files this commit
- tools/startup_window_sample.py (new)
- tools/hdmi_motion_instrument.py (--strict-fps + ERROR17 RED/GREEN)
- arm/misterplexd/media_player.cpp (every-drop + first_video_present)
- scripts/source_rate_rca.sh (default 24/1 provenance)
- .agent-work/w-instr/status.txt
