# w-instr status — SESSION-LATCHED instrumentation

branch: w-avsync-hdmi-measure
# tip after commit

## Parent result (binding)
SESSION-LATCHED DEVICE CONFIRMED:
  1 session × 3 captures: within spread 3.33 ms vs cluster sep 116.89 ms (ratio 35×)
  first flash/beep pair fully separates clusters (n=15, zero overlap)
  common-mode early−late ~25 ms startup transient (both clusters)
  seven predictors dead; peer survey NOT-FOUND; instrument exonerated

## Delivered this turn
1. tools/avsync_measure_hdmi.py
   - live capture: -use_wallclock_as_timestamps 1 on BOTH inputs + -copyts
   - first_pair_offset_ms + t_flash/t_beep (never bare "first")
   - early_window (10s) / late_window (60s) medians + early_minus_late_ms
   - LIMITATIONS + SESSION_LATCHED banner
   - unit 28/28 true rc=0

2. tools/avsync_session_latch.py (NEW)
   - SESSION_LATCHED rc=0 / CAPTURE_RACE_SUSPECT rc=2 / UNSCORED 77
   - parent numbers → SESSION_LATCHED sep_over_within≈35.1 true rc=0

## Parent recipes (host only)
# After N captures in ONE session:
python3 tools/avsync_session_latch.py \
  --values <m1> <m2> <m3> \
  --between-cluster-sep-ms 116.89 --max-within-ms 30 --min-captures 3
echo "true rc=$?"

# Single capture now prints first_pair + early/late:
python3 tools/avsync_measure_hdmi.py --duration 100 --tol-ms 42 --out /tmp/av

# Cluster set:
python3 tools/avsync_bimodality.py --json-dir /path/to/reports --min-n-per-cluster 2
