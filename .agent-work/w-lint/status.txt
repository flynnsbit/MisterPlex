lane: w-lint (GATE-INTEGRITY)
branch: w-lint-gate-integrity
tip: (see git log)

RULE (parent 2026-07-31): absence of evidence must never become a measured value.
  Outcomes: MEASURED_VALUE | MEASURED_ABSENCE | COULD_NOT_MEASURE
  Shell primitive: scripts/lib/measure_status.inc.sh (UNSCORED/NO_DATA family)

PRIORITY FIXES THIS TICK:
1. STA hole — scripts/build_rbf_remote.sh HARD FAIL on absent/unreadable STA
   (was: grep -c || true → zero negative slack). Remote path same.
2. Supervisor TERM trap — plexctl.sh write_supervisor + misterplexd_supervise.sh
   exit 143/130 + SUPERVISE_SIGNAL log (was exit 0 = silent disarm).
3. measure_c2_pixel_path.sh — grep -c || echo 0 → three-way NO_DATA.
4. pidof misterplexd removed from deploy_plex_core, source_rate_rca,
   validate_playback_controls_hw (restore_misterplexd_prev already HARD REFUSE).
5. deploy_misterplexd hook count: no grep -c || true.

RBG suites:
  test_measure_status_three_way.sh     true rc=0
  test_supervise_term_trap.sh          true rc=0  (historic exit0 reproduced; fix→143)
  test_absence_as_zero_static_guard.py true rc=0
  boothook/deploy/rollcall             true rc=0  expected_commands=124

PRIOR (005dd330): (deleted) exe enum, validated-pair 865d4c8a, plexctl exe stop.

PARENT device (you run):
  QUARANTINE_EXECUTE=1 scripts/quarantine_stale_daemon_tree.sh apply
  After deploy: confirm supervise log SUPERVISE_SIGNAL on stop; never exit 0 on TERM
  pair_pin_update.sh --daemon-md5 <full> after validate
