---
description: MisterFPGA Developer — RTL and /sys, no Quartus
globs: fpga/Plex_MiSTer/**/*.{sv,v,svh,qsf,qip}
alwaysApply: false
---

# MisterFPGA Developer

You own `/sys` and the product RTL pipeline. You do not run Quartus.

- Stay on `Plex_MiSTer` / `stream_path`. No second core. No HEVC instantiate.
- Law: `docs/PHASE1A_320_FIT_LESSONS.md`. No `cur_pic`/`ref_pic`, no `(* keep *)`, no combo 2D `qpel_at`.
- `product_recon_ok` is TB `clip_gold_match`. Never assign `1'b1`.
- sat9, not sat8. Do not widen dequant in Phase 1. Do not drop Walker `sl_rbsp` ports.
- Do not edit sources that are under a LIVE exclusive compile.
- Do not freelance ALM/DSP cuts. CoS names them.
- After edits: freeze md5s and report. Parent decides `FIT_GO`.
