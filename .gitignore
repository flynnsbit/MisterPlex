build/
*.o
*.rbf
output_files/
db/
incremental_db/
*.qws
*.bak
.DS_Store
*.sof
*.jic
*.done
*.smsg
*.summary
*.rpt
*.pin
*.pof
*.jdi
*.sld
c5_pin_model_dump.txt
dist/

# Python bytecode caches
__pycache__/
*.pyc

# Lab evidence / captures (not shipped)
captures/
releases/
remote_out/

# Versioned release payloads are intentionally tracked so release packages are
# reproducible from a fresh clone and cannot pick up stale local bitstreams.
!release_artifacts/
!release_artifacts/**
tests/unit/test_input_mailbox

# Generated per-fit build identity (see scripts/gen_build_stamp.py); never committed
# so a stale stamp cannot outlive the source it claims to identify.
fpga/Plex_MiSTer/build_id_stamp.txt
fpga/Plex_MiSTer/build_id.v
