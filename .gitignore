build/
*.o
*.rbf
output_files/
db/
incremental_db/
*.qws
*.bak
.DS_Store
*.sof
*.jic
*.done
*.smsg
*.summary
*.rpt
*.pin
*.pof
*.jdi
*.sld
c5_pin_model_dump.txt
dist/

# Agent scratch (mutations, local notes, temp trees). Not product evidence.
# If a path under here is intentional durable evidence, promote it to
# docs/evidence/ and track it deliberately — do not rely on .agent-work/.
.agent-work/

# Python bytecode caches
__pycache__/
*.pyc

# Lab evidence / captures (not shipped)
captures/
releases/
remote_out/
artifacts/local/

# Matched-pair ARM daemon pins (~30MB ELF). NOT committed deliberately:
# reproducible via scripts/fetch_daemon_pins.sh (parent/device). README stays tracked.
artifacts/daemon-pins/misterplexd.*
!artifacts/daemon-pins/README.md

# Versioned release payloads are intentionally tracked so release packages are
# reproducible from a fresh clone and cannot pick up stale local bitstreams.
!release_artifacts/
!release_artifacts/**
tests/unit/test_input_mailbox
.agent-work/**/*.out
