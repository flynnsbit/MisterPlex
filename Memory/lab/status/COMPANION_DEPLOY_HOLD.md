# Companion / ARM deploy HOLD (2026-08-13)

**HOLD.** Companion/ARM deploy of **any** re-resolve binary is **FORBIDDEN**
until parent writes a **companion deploy token**. Live **10411/10420** stay.
**ZERO bounce.** Soft-skip ≠ PASS. Do **not** invent **READY_TO_DEPLOY=YES**.

**READY_TO_DEPLOY=NO** (T5 EXIT=4 consumed; no new token). **DEPLOY_OK=NO**.
**P4-DISPLAY / P4-720P-MIX** stay **TODO**. No RGB565. No Quartus / menu /
`load_core` / `deploy_misterplexd` / scp RBF / kill / W-flag-off.

true480 **`07f54d9f`** stays. Lab **`ebfe4a12`**.

## Live tree — do not kill

Live **10420** `mpx-main` **FABRIC=1** owns **:3005** (+ UDP 32412). Do **not**
kill. **10411** supervise **FABRIC=1** stays. Do **not** SIGSTOP / rewrite
environ / spawn W-flag-off. Persist watch is FLAG_ABSENT; this tree is not
the reboot default.

Live on-box bin **`9fff2e76`** MATCH_WIRE (W-bin-pin). Leave it.

## W-slot-fix NOFIX — no field-swap to deploy

**W-slot-fix = NOFIX.** `slot.phys` is already `firstPfn<<12`; pageIndex-as-phys
**REFUTED**. There is **no** field-swap binary to scp. Do **not** bounce 10420
for a swap that does not exist (`/tmp/misterplex-agent-W-slot-fix.txt`).

## W-reresolve host-only until parent token

Next host work is **re-resolve** live PFN from `slot.virt` before poke.
**W-reresolve is host-only** until parent writes a deploy token. Local ARM
build ≠ authorize on-box replace of 10420. Token **ABSENT**.

## Forbidden until token

- ZERO additional menu. Do **not** deploy **`17dd3b56`** / **`0bcc6081`**.
- Do **not** `deploy_misterplexd` / scp any re-resolve / kick / size binary.
- T5 EXIT=4 consumed the only menu; do not stamp DEPLOY_OK from SD md5.

## Gates (not PASS)

**POKE_SEEN ≠ FABRIC_PASS.** PLXP src=`0x01200000` UNMAPPED; PLXD fd=0.
**T7 BLOCKED** until FABRIC_PASS + parent token + unique-pfps path.
P3-720P24 stays **IN_PROGRESS / pfps FAIL**. Log REAL ≠ T7 ≠ glass.

## Evidence

`DUAL_MISTERPLEXD_EBFE4A12.md` · `POKE_SEEN_WRONG_SRC.md` · `POKE_SRC_01200000.md`
`/tmp/misterplex-agent-W-slot-fix.txt` · W-bin-pin · W-daemon-maps · W-plxp-ro
`docs/PHASE_BACKLOG.md` P3-720P24 (T5 EXIT=4; ZERO additional menu)
