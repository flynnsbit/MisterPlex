# Glass corroborate landed (parent 2026-08-13)

Leftover PLXJ magic is no longer enough to call the core L4.

`corroborateLiveGlass`: known-L4 pin (`968b828a` disk or `Plex.720p24*` RBFNAME)
→ L4; else True480. `classifyLiveGlass` still word-only for unit.

On-box `fa2f7a7c` after bounce: **`GLASS-MAX glass=true480 bank=640x480 doorbell=0x300FF000`**.
Killed twin 2397. Live **3490** + supervise **3479**. `Plex.rbf` still **07f54d9f**.

`test_present_bank` OK (empty-file MD5 prefix `d41d8cd9`). Not T7 PASS.
