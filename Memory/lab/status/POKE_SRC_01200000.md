# Why PLXP src=0x01200000 (2026-08-13)

**Status:** **RCA_OK.** **POKE_SEEN ≠ FABRIC_PASS.** Soft-skip ≠ PASS.
**T7 NOT_RUN.** P3-720P24 stays **IN_PROGRESS / pfps FAIL**.
**P4-DISPLAY / P4-720P-MIX** stay **TODO**. No RGB565. Do **not** bounce.

Lab **`ebfe4a12`**. true480 **`07f54d9f`**. Live bin **`9fff2e76`** MATCH_WIRE.

## Path (exact)

1. `paintIdle` allocates **once** (`idleFabric_.slot[0].phys == 0`), then
   `idleSrcPhys = slot[0].phys` forever
   (`media_player.cpp` **938-946**, log REAL iff phys≠0 **953-955**).
2. `tryArenaFill` sets `virt = base + pageIndex*4k` and
   `phys = firstPfn<<12` (**427-432**).
   `findCachedContigRuns` **236-238**: `pageIndex=i`, `firstPfn=pfns[i]`.
3. `sendDdrFrame` **1631-1643**: nonzero cached src **skips**
   `resolveCachedSrcPhysFromPagemap`; `pokePlxp(srcPhys)`.
4. `cachedSrcPhysLegal` / `pokePlxp` (**73-88**, **1432-1447**): 8-align +
   `<0x1FF00000` + not bank. **No live PFN check.**
   `0x01200000+1382400=0x01351800` passes → poke **lands**.

## 0x01200000 vs legal slots

`0x01200000 == 0x1200<<12` == 18 MiB == **alloc-time firstPfn 0x1200**.
**No** MATCH_WIRE store `phys = pageIndex<<12`.
`phys == pageIndex<<12` only if `pfns[i]==i` for that run (**UNPROVEN**).

Live 10420 pagemap (W-daemon-maps): legal **`0x0562a000`** / **`0x05a00000`**.
Those are `firstPfn<<12` (`6798<<12=0x01a8e000` ≠ 0x0562a000). Unused
because the `phys!=0` guard never re-fills. PFN `0x1200` **HITS=0**.
arena+`0x01200000` → va `b120a000` phys **`0x1d5b0000`** (offset lookup;
does **not** prove an offset store). Same word is also a 2 MiB-buddy phys.

Standalone probe (same header) stored `0x03c00000` / `0x04500000` — cannot
be pageIndex<<12 (60 MiB > 32 MiB arena).

## Do not

- Do **not** “swap pageIndex/firstPfn” as the product fix.
- Do **not** bounce / kill 10411/10420 / W-flag-off.
- **POKE_SEEN ≠ FABRIC_PASS ≠ T7.** P4 stay **TODO**.

## Evidence

`/tmp/misterplex-agent-W-slot-rca.txt` · W-daemon-maps · W-plxp-ro · W-bin-pin
