# Unused profile drafts

`plex_resolve.cpp` still sends `X-Plex-Client-Profile-Name: MiSTerPlex`.
Live file is `MiSTerPlex.xml` (640x480 Baseline + AAC MPEG-TS).

| File | Status |
|---|---|
| `MiSTerPlex.xml` | **Live** 640x480 Baseline |
| `MiSTerPlex_720p.xml` | Unused. Bank when 720p CBP H.264 FPGA is real |
| `MiSTerPlex_HEVC.xml` | Unused. Bank when HEVC RBF is real (Main 8-bit I/P 720p) |

Do not copy these over the live PMS profile until Chief of Staff says so.
