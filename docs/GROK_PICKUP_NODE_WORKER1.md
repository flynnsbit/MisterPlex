# Pickup (public stub)

Full lab session + tokens live in the **private** tree path
`Memory/lab/status/GROK_PICKUP_NODE_WORKER1.md`
(gitignored; copy the worktree including `Memory/` to node-worker1).

Do not push `Memory/` or lab tokens to GitHub.
