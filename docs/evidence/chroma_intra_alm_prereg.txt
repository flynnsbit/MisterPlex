# ALM / resource prereg — rtl/chroma-intra (sv-mvd lane)
# SOURCE_SHA=4d89bd93 (Intra-in-P fix) on 788aa5f base
# Updated after parent M10K physical-block correction (2026-07-30).

## RELIANCE ON BAD GUIDANCE — PLAIN ADMISSION
This lane's prior prereg (same file, earlier revision) stated:
  "Base ... M10K free ~46%"
  and planned chroma ALM wins via:
  "Chroma plane / neighbour rows → M10K (h264_byte_ram_sp pattern) | ALM→RAM | -2k .. -8k"
  "PREREG whole-device ... if M10K+serial applied fully: ALMs 38k..43k (mid ~40.5k)"

That "~46% free" was **bit-capacity arithmetic**, not physical M10K **block** headroom.
Parent audit of live product-wire6 fit (14eaeff3):
  RAM blocks: 465/553 (84%) → only **88 physical blocks** remain.
I relied on the bit figure for the chroma-ship area plan. **That plan is invalidated
for further M10K conversions until sv-integrate quotes physical block counts.**

## Already-present M10K in this lane's tree (stand; do not add more)
From sink (h264_i_res_recon_sink.sv) — already committed / bit-exact work:
  u_plane_y, u_plane_u, u_plane_v, u_top_row, u_top_u, u_top_v  (h264_byte_ram_sp)
Block cost of these: **unknown this lane** — await sv-integrate quote from map/fit reports.
No new M10K conversion will be started by this lane until that report.

## DSP — hold shared-serial-IQ absolutely
Code (decode_stub.sv):
  - exactly ONE h264_dequant4x4_serial u_shared_dq
  - sink EXT_SERIAL_DQ=1; p_chr always ext_dq_* (no private mul)
Live fit (parent quote, product-wire6): DSP 74/112.
Probe-tree "111/112" is **not** the shipping margin claim.
Any new multiplier = **blocker**, not a cost trade.
Chroma DC Hadamard +8 DSP scenario (docs/chroma-merge-area-bound.md) remains a
scenario, not a merged measurement — serial IQ is what keeps merge survivable.

## Functional (sim, true rc=0) — unchanged by this correction
BEFORE residual: ON 1765 / OFF 1773
AFTER  ON:  HEADLINE intra=300/300 inter=1962/3300
AFTER  OFF: HEADLINE intra=300/300 inter=1960/3300
PREREG residual HI 1939 miss: ON 1962 > ceiling (cascade >> 8 local Intra-in-P MBs)
Unit: idct rc=0 recon_sig=0x3b; inter rc=0; multinal rc=0

## Revised area stance (no fit this lane; no map this lane)
- Intra-in-P control-only delta still: **+200..+800 ALM** (FSM/regs; no new RAM, no new mul)
- stream_path acc_v + hold_v clear: **+20..+80 ALM**
- **Withdrawn:** "-2k..-8k via chroma→M10K" until physical block headroom known
- **Withdrawn:** whole-device mid ~40.5k predicated on further M10K packing
- Ship composition ALM/DSP/M10K-blocks: **sv-integrate / map lane owns**; this lane does not invent BUILD_OK

## Pause / continue
PAUSE: any further array→M10K conversion on this lane
CONTINUE: correctness (Intra-in-P done @ 4d89bd93); shared-serial IQ discipline
NO FIT / NO RBF / NO DEPLOY this lane
