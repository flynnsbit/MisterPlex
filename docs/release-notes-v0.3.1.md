# MiSTerPlex v0.3.1

Minor fix on the stable 320×240 cast release.

## Fix

- **Play icon points right** — the transport overlay “play” triangle was drawn pointing left; it now faces the normal play direction.

## Same as v0.3.0

- On-screen playback bar, local keyboard/gamepad controls, cast sync, PMS progress
- Content bank 320×240 · install defaults: `PRESENT=both`

## Package identity

| File | MD5 |
|------|-----|
| `cores/Plex.rbf` | `41adb98c7a630b541091c22ce291be68` (unchanged) |
| `bin/misterplexd` | (see MD5SUMS — rebuilt with overlay fix only) |
