--- a/rtl/ddr_frame_store.sv
+++ b/rtl/ddr_frame_store.sv
@@ -481,15 +481,47 @@
 	wire signed [11:0] y_limited = y_s - 12'sd16;
 	wire signed [20:0] y_scaled = 21'sd298 * y_limited;
 	wire signed [20:0] r_calc_w, g_calc_w, b_calc_w;
+	wire pixel_update = RUNTIME_GEOMETRY ?
+		(rd_active_r && rd_visible_r && has_frame && !miss_d && y_hit_r) :
+		((rd_active_d || !rd_active) && rd_visible_d && has_frame && !miss_d && y_hit_r);
+	wire pixel_clear = RUNTIME_GEOMETRY || !has_frame || !rd_visible_d || miss_d;
+	wire pixel_de = rd_active_r && rd_visible_r && has_frame && !generation_clear;
+	wire colour_update, colour_clear, colour_de;
 	generate
 		if (LIMITED_BT601) begin : g_limited_bt601
-			assign r_calc_w = y_scaled + (21'sd409 * v_s) + 21'sd128;
-			assign g_calc_w = y_scaled - (21'sd100 * u_s) - (21'sd208 * v_s) + 21'sd128;
-			assign b_calc_w = y_scaled + (21'sd516 * u_s) + 21'sd128;
+			reg signed [20:0] y_product, rv_product, gu_product, gv_product, bu_product;
+			reg update_r, clear_r, de_r;
+			always @(posedge clk) begin
+				if (reset || generation_hold) begin
+					update_r <= 1'b0;
+					clear_r <= 1'b1;
+					de_r <= 1'b0;
+				end else begin
+					// Independent products break the RAM/mux-to-cascaded-MAC
+					// cone. Carry the original update/black/hold decision too.
+					y_product <= y_scaled;
+					rv_product <= 21'sd409 * v_s;
+					gu_product <= 21'sd100 * u_s;
+					gv_product <= 21'sd208 * v_s;
+					bu_product <= 21'sd516 * u_s;
+					update_r <= pixel_update;
+					clear_r <= pixel_clear;
+					de_r <= pixel_de;
+				end
+			end
+			assign r_calc_w = y_product + rv_product + 21'sd128;
+			assign g_calc_w = y_product - gu_product - gv_product + 21'sd128;
+			assign b_calc_w = y_product + bu_product + 21'sd128;
+			assign colour_update = update_r && !generation_hold;
+			assign colour_clear = clear_r || generation_hold;
+			assign colour_de = de_r && !generation_clear && !generation_hold;
 		end else begin : g_legacy_full_range
 			assign r_calc_w = (y_ext <<< 8) + (21'sd359 * v_s);
 			assign g_calc_w = (y_ext <<< 8) - (21'sd88 * u_s) - (21'sd183 * v_s);
 			assign b_calc_w = (y_ext <<< 8) + (21'sd454 * u_s);
+			assign colour_update = pixel_update;
+			assign colour_clear = pixel_clear;
+			assign colour_de = pixel_de;
 		end
 	endgenerate
 	wire signed [11:0] r_calc = r_calc_w[19:8];
@@ -576,23 +608,12 @@
 
 			// The FPGA path exports the cropped aperture with the RAM/RGB latency.
 			// Allocation padding must not become part of the scaler's source DAR.
-			rd_de <= rd_active_r && rd_visible_r && has_frame && !generation_clear;
-			if (RUNTIME_GEOMETRY) begin
-				if (rd_active_r && rd_visible_r && has_frame && !miss_d && y_hit_r) begin
-					rd_r <= sat8(r_calc);
-					rd_g <= sat8(g_calc);
-					rd_b <= sat8(b_calc);
-				end else begin
-					rd_r <= 8'd0;
-					rd_g <= 8'd0;
-					rd_b <= 8'd0;
-				end
-			// Legacy keepv RGB/blank behavior is unchanged.
-			end else if ((rd_active_d || !rd_active) && rd_visible_d && has_frame && !miss_d && y_hit_r) begin
+			rd_de <= colour_de;
+			if (colour_update) begin
 				rd_r <= sat8(r_calc);
 				rd_g <= sat8(g_calc);
 				rd_b <= sat8(b_calc);
-			end else if (!has_frame || !rd_visible_d || miss_d) begin
+			end else if (colour_clear) begin
 				rd_r <= 8'd0;
 				rd_g <= 8'd0;
 				rd_b <= 8'd0;
--- a/rtl/h264_i16_dc_hadamard.sv
+++ b/rtl/h264_i16_dc_hadamard.sv
@@ -1,6 +1,7 @@
 // Intra 16x16 luma DC: inv-quant + Hadamard (ITU 8.5.10).
 // Bit-exact to host cavlc::invQuantHadamardDc4x4 / FFmpeg ff_h264_luma_dc_dequant_idct.
-// One multiplier, 16 cycles. Do not sat9 dc_out.
+// One shared multiplier; registered butterflies, product and scale retirement.
+// All 16 outputs retire before done. Do not sat9 dc_out.
 
 `default_nettype none
 
@@ -48,13 +49,18 @@
 	integer z0, z1, z2, z3;
 	reg signed [5:0] factor;
 	reg signed [15:0] input_t [0:15];
-	reg signed [15:0] coeff_latched [0:15];
-	reg [5:0] qp_latched;
 	reg signed [17:0] temp [0:15];
+	reg signed [17:0] row_r [0:15];
 	reg signed [19:0] zmul [0:15];
-	reg signed [19:0] z_sel;
-	wire signed [25:0] product = z_sel * factor;
-	reg signed [35:0] p;
+	reg signed [19:0] transform_r [0:15];
+	reg [3:0] qp_shift;
+	wire signed [25:0] product = transform_r[mi] * factor;
+	reg signed [25:0] product_r;
+	reg signed [35:0] scaled_r;
+	wire signed [35:0] rounded = (scaled_r + 36'sd2) >>> 2;
+	reg product_valid, scaled_valid;
+	reg [3:0] product_index, scaled_index;
+	reg columns_pending, issued_all;
 	reg        busy;
 	reg [3:0]  mi;
 
@@ -63,7 +69,7 @@
 			input_t[i] = 16'sd0;
 		for (i = 0; i < 16; i = i + 1) begin
 			t = transpose_z(zz_i(i));
-			input_t[t] = coeff_latched[i];
+			input_t[t] = coeff_scan[i];
 		end
 
 		for (i = 0; i < 4; i = i + 1) begin
@@ -77,40 +83,61 @@
 			temp[4*i+3] = z1 + z2;
 		end
 
-		factor = 6'(mf0_of(qp_latched));
-
 		for (i = 0; i < 4; i = i + 1) begin
-			z0 = temp[0+i] + temp[8+i];
-			z1 = temp[0+i] - temp[8+i];
-			z2 = temp[4+i] - temp[12+i];
-			z3 = temp[4+i] + temp[12+i];
+			z0 = row_r[0+i] + row_r[8+i];
+			z1 = row_r[0+i] - row_r[8+i];
+			z2 = row_r[4+i] - row_r[12+i];
+			z3 = row_r[4+i] + row_r[12+i];
 			zmul[i*4+0] = z0 + z3;
 			zmul[i*4+1] = z1 + z2;
 			zmul[i*4+2] = z1 - z2;
 			zmul[i*4+3] = z0 - z3;
 		end
-		z_sel = zmul[mi];
-		p = 36'(product) <<< (qp_latched/6);
-		p = (p+36'sd2) >>> 2;
 	end
 
 	always @(posedge clk) begin
 		done <= 1'b0;
+		product_valid <= 1'b0;
+		scaled_valid <= product_valid;
 		if (reset) begin
 			busy <= 1'b0;
 			mi   <= 4'd0;
+			columns_pending <= 1'b0;
+			issued_all <= 1'b0;
+			product_valid <= 1'b0;
+			scaled_valid <= 1'b0;
 		end else if (start && !busy) begin
 			busy <= 1'b1;
 			mi   <= 4'd0;
-			qp_latched <= qp;
-			for (integer j=0;j<16;j=j+1) coeff_latched[j]<=coeff_scan[j];
+			columns_pending <= 1'b1;
+			issued_all <= 1'b0;
+			factor <= 6'(mf0_of(qp));
+			qp_shift <= 4'(qp / 6);
+			// Four signed16 inputs fit signed18; the full transform fits
+			// signed20. Keep the original 26/36/32-bit dequant arithmetic.
+			for (integer j=0;j<16;j=j+1) row_r[j] <= temp[j];
 		end else if (busy) begin
-			dc_out[mi] <= p[31:0];
-			if (mi == 4'd15) begin
-				busy <= 1'b0;
-				done <= 1'b1;
-			end else
-				mi <= mi + 4'd1;
+			if (columns_pending) begin
+				for (integer j=0;j<16;j=j+1) transform_r[j] <= zmul[j];
+				columns_pending <= 1'b0;
+			end else if (!issued_all) begin
+				product_r <= product;
+				product_index <= mi;
+				product_valid <= 1'b1;
+				if (mi == 4'd15) issued_all <= 1'b1;
+				else mi <= mi + 4'd1;
+			end
+			if (product_valid) begin
+				scaled_r <= 36'(product_r) <<< qp_shift;
+				scaled_index <= product_index;
+			end
+			if (scaled_valid) begin
+				dc_out[scaled_index] <= rounded[31:0];
+				if (scaled_index == 4'd15) begin
+					busy <= 1'b0;
+					done <= 1'b1;
+				end
+			end
 		end
 	end
 endmodule
--- a/rtl/h264_inter_pred.sv
+++ b/rtl/h264_inter_pred.sv
@@ -48,7 +48,10 @@
 		input signed [15:0] b,
 		input signed [15:0] c
 	);
-		median3 = a + b + c - min2(min2(a, b), c) - max2(max2(a, b), c);
+		// Three independent signed comparisons select the actual middle
+		// operand, including signed16 extremes, without a sum/min/max chain.
+		median3 = (a > b) ? ((b > c) ? b : ((a > c) ? c : a)) :
+		                      ((a > c) ? a : ((b > c) ? c : b));
 	endfunction
 
 	function automatic signed [15:0] pred_one(
--- a/rtl/h264_mb_ctrl.sv
+++ b/rtl/h264_mb_ctrl.sv
@@ -648,8 +648,10 @@
 	wire signed [31:0] i16_dc [0:15];
 	reg  hdc_start;
 	wire hdc_done;
+	wire hdc_reset = reset || vcl_pulse || frame_abort ||
+	                 phase == ST_IDLE || phase == ST_STOP;
 	h264_i16_dc_hadamard u_i16_dc (
-		.clk(clk), .reset(reset), .start(hdc_start),
+		.clk(clk), .reset(hdc_reset), .start(hdc_start),
 		.coeff_scan(i16_dc_scan),
 		.qp(lat_qp),
 		.dc_out(i16_dc),
@@ -663,7 +665,7 @@
 	end
 	reg hdc_valid;
 	always @(posedge clk) begin
-		if (reset)
+		if (hdc_reset)
 			hdc_valid <= 1'b0;
 		else if (hdc_start)
 			hdc_valid <= 1'b0;
@@ -916,7 +918,9 @@
 		.fetch_mb_y(STATIC_IDR_ONLY ? 8'd0 : dpb_fmb_y),
 		.fetch_part_mode(pdec_mode), .fetch_part_idx(2'd0),
 		.fetch_part_w(pdec_w), .fetch_part_h(pdec_h),
-		.fetch_mv_x_qpel(mv_xw), .fetch_mv_y_qpel(mv_yw),
+		// The kick and complete motion result were accepted together.
+		// Do not extend live MVP/MVD arithmetic into the DPB request stage.
+		.fetch_mv_x_qpel(mv_kick_x), .fetch_mv_y_qpel(mv_kick_y),
 		.fetch_busy(dpb_fetch_busy), .fetch_done(dpb_fetch_done),
 		.fetch_error_no_ref(dpb_fetch_err),
 		.luma_frac_x(dpb_lfx), .luma_frac_y(dpb_lfy),
--- a/rtl/present_core.sv
+++ b/rtl/present_core.sv
@@ -548,10 +548,15 @@
 
 	wire [7:0] fr, fg, fb;
 	wire frame_de;
-	reg [2:0] pixel_valid_pipe;
+`ifdef DDR_FRAME_STORE
+	localparam int STORE_COLOUR_LATENCY = FPGA_DECODE_320 ? 1 : 0;
+`else
+	localparam int STORE_COLOUR_LATENCY = 0;
+`endif
+	reg [2+STORE_COLOUR_LATENCY:0] pixel_valid_pipe;
 	always @(posedge clk) begin
 		if (reset) pixel_valid_pipe <= 0;
-		else pixel_valid_pipe <= {pixel_valid_pipe[1:0], ce_pix_i};
+		else pixel_valid_pipe <= {pixel_valid_pipe[1+STORE_COLOUR_LATENCY:0], ce_pix_i};
 	end
 	wire       has_frame;
 	wire       swap_pending;
@@ -771,16 +776,39 @@
 		hb_sr <= {hb_sr[DE_LAG-2:0], hb};
 		hs_sr <= {hs_sr[DE_LAG-2:0], hs};
 	end
-	wire hb_d = hb_sr[DE_LAG-1];
-	wire hs_d = hs_sr[DE_LAG-1];
-	wire vb_d = vb | past_last_row;
-	wire vs_d = vs;
+	wire hb_d, hs_d, vb_d, vs_d;
+	wire [7:0] br_d, bg_d, bb_d;
+	generate
+		if (STORE_COLOUR_LATENCY != 0) begin : g_colour_return_tags
+			reg hb_r, hs_r, vb_r, vs_r;
+			reg [7:0] br_r, bg_r, bb_r;
+			always @(posedge clk) begin
+				if (reset) begin
+					hb_r <= 1'b1; vb_r <= 1'b1;
+					hs_r <= 1'b0; vs_r <= 1'b0;
+					br_r <= 0; bg_r <= 0; bb_r <= 0;
+				end else begin
+					hb_r <= hb_sr[DE_LAG-1];
+					hs_r <= hs_sr[DE_LAG-1];
+					vb_r <= vb | past_last_row;
+					vs_r <= vs;
+					br_r <= br; bg_r <= bg; bb_r <= bb;
+				end
+			end
+			assign hb_d = hb_r, hs_d = hs_r, vb_d = vb_r, vs_d = vs_r;
+			assign br_d = br_r, bg_d = bg_r, bb_d = bb_r;
+		end else begin : g_original_return_tags
+			assign hb_d = hb_sr[DE_LAG-1], hs_d = hs_sr[DE_LAG-1];
+			assign vb_d = vb | past_last_row, vs_d = vs;
+			assign br_d = br, bg_d = bg, bb_d = bb;
+		end
+	endgenerate
 
 	// Gate the frame pixels with EXACTLY the delayed signal that drives VGA_DE.
 	wire de_out = (FPGA_DECODE_320 && use_ext) ? frame_de : (~hb_d & ~vb_d);
-	wire [7:0] leg_r = use_ext ? (de_out ? fr : 8'd0) : (show_pattern ? br : 8'd0);
-	wire [7:0] leg_g = use_ext ? (de_out ? fg : 8'd0) : (show_pattern ? bg : 8'd0);
-	wire [7:0] leg_b = use_ext ? (de_out ? fb : 8'd0) : (show_pattern ? bb : 8'd0);
+	wire [7:0] leg_r = use_ext ? (de_out ? fr : 8'd0) : (show_pattern ? br_d : 8'd0);
+	wire [7:0] leg_g = use_ext ? (de_out ? fg : 8'd0) : (show_pattern ? bg_d : 8'd0);
+	wire [7:0] leg_b = use_ext ? (de_out ? fb : 8'd0) : (show_pattern ? bb_d : 8'd0);
 
 `ifdef PRESENT_MULTI_PIXEL
 	// ------------------------------------------------------------------
@@ -851,7 +879,7 @@
 	wire [PRESENT_PPC-1:0]   mp_npx_lv = mp_lane_de & {PRESENT_PPC{has_frame}};
 
 	// Align store response: 1 (store_x reg) + typical ddr RGB pipe ≈ 4.
-	localparam int MP_STORE_LAT = 4;
+	localparam int MP_STORE_LAT = 4 + STORE_COLOUR_LATENCY;
 	reg [MP_STORE_LAT-1:0] mp_tq_v;
 	reg mp_tq_hb [0:MP_STORE_LAT-1];
 	reg mp_tq_hs [0:MP_STORE_LAT-1];
@@ -967,8 +995,9 @@
 	assign g = leg_g;
 	assign b = leg_b;
 	// With sparse CE, expose the beat belonging to the registered coordinate
-	// plus the two-cycle native RAM/RGB response, not the request's early CE.
-	assign ce_pix = (`PLEX_PERF_SYS_MULT > 1) ? pixel_valid_pipe[2] : ce_pix_i;
+	// plus RAM/colour response stages, not the request's early CE.
+	assign ce_pix = (`PLEX_PERF_SYS_MULT > 1) ?
+		pixel_valid_pipe[2+STORE_COLOUR_LATENCY] : ce_pix_i;
 	assign de_pix = de_out;
 	assign HBlank = hb_d;
 	assign HSync  = hs_d;
