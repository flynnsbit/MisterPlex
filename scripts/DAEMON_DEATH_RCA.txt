DAEMON DEATH / HOOK / rc=7 RCA — w-bounce2
Date: 2026-07-30 (updated post v8b window)
v8b 4cbee6ff live (hardening + doorbell). Field deaths still UNEXPLAINED (B3 miss).
Rollback armed: v7 703b2fd4; v5 a56bbc3c behind. Hybrid owns v5-vs-v7 novelty.

================================================================
0. COORDINATION (parent correction — accepted)
================================================================
I messaged hybrid "device FREE / your slot" without parent gate. That produced
excellent CPU numbers but caused a false unauthorised-breach accusation.
Rule going forward: bounce2 does not sub-delegate device ownership without
telling parent first. Acknowledged.

================================================================
1. FIELD EVIDENCE — what actually terminated processes
================================================================
Separate intentional kills from unexplained exits. Rule 0: no cause without
exit status / signal / quoted log / artifact.

A) INTENTIONAL lab kills (PROVEN — script source)
| when (CDT approx) | actor | mechanism | evidence |
|-------------------|-------|-----------|----------|
| 02:21 deploy | bounce2 v7_claim_install | kill $(pidof misterplexd) then nohup | build/v7_claim_install.sh:43-45 |
| 02:23 MENU A/B | bounce2 remote_v7_menu_fire | soft_stop kill | build/remote_v7_menu_fire.sh |
| 02:25 cast PROFILE on | bounce2 remote_v7_cast_cpu_profile | soft_stop | build/remote_v7_cast_cpu_profile.sh:41 |
| 02:27 cast PROFILE off | bounce2 same | soft_stop restore | cast_cpu_profile.sh:115 |
| 02:33 remeasure | bounce2 remote_v7_remeasure_clean | soft_stop x2 | remeasure_clean.sh:59,162 |
| every soft bounce ensure_daemon wrong-id path | killall misterplexd | mister_soft_bounce.sh:609 |

These are SIGTERM (default kill), handled by main.cpp on_signal → g_stop → return 0.
They look like "daemon died" in ps snapshots. They are not product crashes.

B) HYBRID-OBSERVED UNEXPLAINED EXIT (field) — ONE clear spontaneous gap
Hybrid RESULT M1 + daemon_death.txt (mtime 02:31:04):
  Last log lines before empty PS (quoted from daemon_death.txt):
    "misterplexd: resolve failed: no such item on PMS (/library/metadata/11 …) — test pattern"
    "misterplexd: PLAY testsrc off=0 dur=120000"
    "media: audio pump end bytes=17898240 f2=0"
    "media: session end frames=2795 recon=0 cabac=0 stream=off rawvideo=on present=fpga skip_rgb=0"
    then initPresent-class lines:
    "media: FPGA frame path OK (PRESENT=fpga → DDR YUV420p)"
    "media: DDR bitstream CTRL=PLXD (STREAM=0, producer dormant)"
    "media: audio device /dev/MrAudio OK …"
  ===PS=== empty (no misterplexd)
  ===OOM=== empty section
  dmesg: only MiSTer_fb / virtual input — no oom-killer, no segfault line in capture

PID timeline (hybrid artifacts):
  pids_start 24514 @~02:26
  cpu_steady mpid=25598 still alive t0→t1 @02:29:06 (same_mpid end)
  daemon_death PS empty @02:31:04
  daemon_restart pid=26292 @02:31:26 (hybrid soft restart #1)
  later cpu_ab_restart2 pid=28574 @02:35 (after bounce2 remeasure leave — restart #2 context)

Exit status / signal for the 02:29→02:31 gap: **NOT CAPTURED**.
  nohup does not record $?; no wrapper waited on the child.
  Therefore: **UNKNOWN what terminated it** (signal vs clean exit vs crash).

C) Code that rules mechanisms IN/OUT (quoted, not guessed)
1. "session end" alone does NOT exit the process.
   media_player.cpp ~3131 logs "media: session end …" then returns from threadMain;
   main.cpp:1210 `while (!g_stop.load())` keeps HTTP companion alive until SIGINT/SIGTERM.
2. SIGPIPE silent kill was a known historical footgun; main.cpp:410-415 installs
   `std::signal(SIGPIPE, SIG_IGN)` with explicit comment that default terminates silently.
   So classic pipe-on-seek death is **mitigated in this binary** (still not proof this
   death was not something else).
3. threadMain exceptions are caught (media_player.cpp:1092-1098) → log + playing=false,
   not process exit.
4. startIdle/stopIdle historically could std::terminate on thread move race; now
   guarded by idleMu_ (media_player.cpp:681-714). **Whether a remaining terminate
   path exists is UNKNOWN** without a controlled catch of WIFSIGNALED/abort.
5. resolve-failed → testsrc is intentional fallback (main.cpp:676-684), not an exit.

D) Shared signature across "deaths"?
| event | last product log | OOM | exit code | intentional kill nearby? |
|-------|------------------|-----|-----------|---------------------------|
| lab soft_stop/deploy | truncated by `: >log` often | n/a | SIGTERM expected | YES |
| hybrid M1 gap | session end + initPresent lines | no in capture | UNKNOWN | bounce2 claim ended 02:27:14; gap is 02:29-02:31 — **not** bounce2 soft_stop |
| hybrid restart #2 | after further A/B | n/a | hybrid action | intentional restart |

Conclusion field:
  - Most process disappearances during AFTER are **lab soft_stop / bounce / hybrid restart**.
  - **At least one** disappearance (post cpu_steady 25598 → empty PS) is **not explained**
    by bounce2 soft_stop timing and has **no captured exit status**.
  - Hypothesis "resolve→testsrc session end" is **INSUFFICIENT**: session end is normal;
    code does not exit there. Parent is correct that "no OOM" only rules out OOM.
  - Settling check (NEEDS parent window; do not run unprompted):
      nohup sh -c './bin/misterplexd ... & echo $! > /run/misterplexd.pid; wait $(cat /run/misterplexd.pid); echo EXIT:$? WIFSIGNALED...' 
    or `strace -f -e trace=exit_group,killed` / core pattern — capture rc/signal on next death.

================================================================
2. RECURRENCE
================================================================
NOT ATTEMPTED. Parent: ask first, schedule window. Standing by.

Pre-registered repro (for when authorised):
  R1 leave daemon 703b2fd4 --id misterplex-dev CORENAME=Plex PROFILE=0
  R2 single playMedia metadata/11 stick 90s; no concurrent foreign SSH
  R3 second playMedia same key mid-play (hybrid trigger shape)
  R4 force resolve-fail path (bad key) → expect testsrc + session end + **process still up**
  R5 port 3005 /resources still answers after R3/R4
  FAIL if process gone without our SIGTERM; capture wait rc.

================================================================
3. BOOT HOOK SUPERVISION (static measure of installed design)
================================================================
Installed line (STARTUP_HOOK_DONE.txt — both _user-startup.sh and user-startup.sh):
  /media/fat/misterplex/bin/misterplexd --name MiSTerPlex --id misterplex-dev \
    --port 3005 --conf /media/fat/misterplex/misterplex.conf >>…log 2>&1 &

Properties (static, PROVEN from hook text + deploy_misterplexd.sh LINE):
| property | result | method |
|----------|--------|--------|
| Starts on boot | yes (one shot &) | hook line present |
| Restarts on exit | **NO** | no while/supervise/respawn; bare background |
| systemd/procd unit | **NO** | MiSTer user-startup only |
| Logs exit code | **NO** | nohup/redirect only |
| After mid-session death | cast target **gone** until manual start or reboot | consequence of no supervise |

User impact if unexplained death is real:
  boot → daemon up → cast works → process exits → Plex client loses player with
  no OSD reason → reboot brings it back once. Matches "vanishes" class.

Recommendation (design only until parent auths device change):
  Option A: hook wrapper
    while true; do misterplexd ... >>log 2>&1; ec=$?; echo "exit $ec at $(date)" >>log; sleep 2; done &
  Option B: tiny supervise script + hook calls it
  Must keep --id misterplex-dev; backoff; no kill -9; do not thrash on crashloop.
  **Do not install without parent auth.**

E2E proof that death leaves target gone: NEEDS device window (kill -TERM once,
  assert /resources fails, hook does not restart within N seconds). Static text
  already proves no restart path exists.

================================================================
4. DEPLOY rc=7 ("resources id") — root cause + fix direction
================================================================
Code path (build/v7_claim_install.sh + scripts/DEPLOY_V7_RUNBOOK.sh + deploy_misterplexd.sh):
  nohup misterplexd ... &
  sleep 1.0                    # v7 install / runbook
  ps … --id check              # passed (daemon_ps had id)
  wget -qO- http://127.0.0.1:3005/resources once
  grep machineIdentifier="misterplex-dev" || exit 7

claim-audit: prior_ec=7 on w-bounce2-v7 release 02:21:40; ensure_daemon_ok on
cleanup immediately after with id=misterplex-dev.

Interpretation with evidence:
  - exit 7 is specifically resources/id gate (install.sh:80-83), not md5 (exit 2)
    and not "not running" (deploy_misterplexd exit 6).
  - ps id check passed before resources (order in script) ⇒ process up, HTTP not ready
    OR wget empty once.
  - sleep 1.0 single-shot is the race window (companion bind + first /resources).

Fix (code, no device required to land):
  Retry /resources up to e.g. 15× 200ms (3s) after ps id OK; log each attempt;
  exit 7 only if still missing. Keep exit 7 meaning as real id mismatch after ready.
  Same pattern in deploy_misterplexd.sh and DEPLOY_V7_RUNBOOK.sh / v7_claim_install.sh.

================================================================
5. WHAT IS NOT THIS LANE
================================================================
- v5 vs v7 novelty of death: hybrid pre-registered prediction — do not duplicate.
- No rollback. No device touch until parent authorises repro/supervise install.

================================================================
6. OPEN CHECKS (honest UNKNOWN)
================================================================
| question | status | settling check |
|----------|--------|----------------|
| Exact signal/rc of 02:29-02:31 death | UNKNOWN | wait-wrapper or strace on next death |
| Did initPresent lines belong to dying or new process? | UNKNOWN | PID in log line or /proc before/after |
| Does testsrc path ever abort? | UNKNOWN code-path audit incomplete | controlled R4 above |
| Idle/thread terminate residual? | UNKNOWN | abort backtrace if WIFSIGNALED SIGABRT |
| Recurrence rate | NOT RUN | parent-scheduled R1-R5 |

================================================================
7. HYBRID HOST HALF SYNTHESIS (2026-07-30T02:47) — bounce2 field half
================================================================
Hybrid commit 568a2c70 + RESULT.txt: v7 did NOT introduce exit.
Mechanism matches our field log shape and our static code read:

  threadMain session end: paintIdle(); startIdle(); log("session end")
  play(): stopIdle() at entry → join thr_ (restarts idle) → initPresent
          WITHOUT second stopIdle → fpga.open unmaps while idle may publish

Field (daemon_death.txt) order is exactly:
  resolve-fail → testsrc → session end → "FPGA frame path OK" (initPresent L~806)
  → process gone. "FPGA frame path OK" ≠ idle paint ("idle FPGA frame path OK").

bounce2 shipped play() confirmed missing post-join stopIdle (media_player.cpp
~1057-1060 before this update). stop() already had post-join stopIdle (~960).

================================================================
8. DEVICE FALSIFY + SHIP STATUS (parent 2026-07-30 — binding wording)
================================================================
B3 falsify on live v7 703b2fd4 with waitpid wrapper (re-entrant playMedia,
resolve-fail, concurrent storm): **:3005 STAYED UP**. No WRAP_EXIT.
Signal still **UNCAPTURED**. Death **did not reproduce**.

Therefore the causal claim is **NOT PROVEN**:
  - idle×initPresent race: REAL in source; CLOSED as **hardening** (v8b
    4cbee6ff = 1e27506 stopIdle+presentMu + 2a4e406 doorbell promote).
  - "Race could explain deaths" ≠ "we found the cause of the deaths".
  - Observed field daemon deaths remain **UNEXPLAINED**.
  - Do **not** record v8 as having fixed the crash.
  - Publishing the failed reproduction is the standing fact if death recurs.

Supervise hook installed (backoff + SUPERVISE_EXIT log) so a future death is
visible rather than silent vanish — does not prove root cause.

Rollback v7 for the race alone: NOT indicated (agree hybrid causality).
False freeze 98e8c8da (missing doorbell) was rolled back on hard bars.
